/**
 * Nudm_SDM
 * Nudm Subscriber Data Management Service. � 2020, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC). All rights reserved. 
 *
 * The version of the OpenAPI document: 2.1.2
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 5.1.1-SNAPSHOT.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ScheduledCommunicationType.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {



ScheduledCommunicationType::ScheduledCommunicationType()
{
}

ScheduledCommunicationType::~ScheduledCommunicationType()
{
}

void ScheduledCommunicationType::validate()
{
    // TODO: implement validation
}

web::json::value ScheduledCommunicationType::toJson() const
{
    web::json::value val = web::json::value::object();

    
    if (m_value == eScheduledCommunicationType::ScheduledCommunicationType_DOWNLINK_ONLY) val = web::json::value::string(U(DOWNLINK_ONLY));
    if (m_value == eScheduledCommunicationType::ScheduledCommunicationType_UPLINK_ONLY) val = web::json::value::string(U(UPLINK_ONLY));
    if (m_value == eScheduledCommunicationType::ScheduledCommunicationType_BIDIRECTIONAL) val = web::json::value::string(U(BIDIRECTIONAL));

    return val;
}

bool ScheduledCommunicationType::fromJson(const web::json::value& val)
{
    auto s = val.as_string();

    
    if (s == utility::conversions::to_string_t(DOWNLINK_ONLY)) m_value = eScheduledCommunicationType::ScheduledCommunicationType_DOWNLINK_ONLY;
    if (s == utility::conversions::to_string_t(UPLINK_ONLY)) m_value = eScheduledCommunicationType::ScheduledCommunicationType_UPLINK_ONLY;
    if (s == utility::conversions::to_string_t(BIDIRECTIONAL)) m_value = eScheduledCommunicationType::ScheduledCommunicationType_BIDIRECTIONAL;
    return true;
}

void ScheduledCommunicationType::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    utility::string_t s;

    
    if (m_value == eScheduledCommunicationType::ScheduledCommunicationType_DOWNLINK_ONLY) s = utility::conversions::to_string_t(DOWNLINK_ONLY);
    if (m_value == eScheduledCommunicationType::ScheduledCommunicationType_UPLINK_ONLY) s = utility::conversions::to_string_t(UPLINK_ONLY);
    if (m_value == eScheduledCommunicationType::ScheduledCommunicationType_BIDIRECTIONAL) s = utility::conversions::to_string_t(BIDIRECTIONAL);

    multipart->add(ModelBase::toHttpContent(namePrefix, s));
}

bool ScheduledCommunicationType::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }
    {
        utility::string_t s;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), s);
        eScheduledCommunicationType v;

        
        if (s == utility::conversions::to_string_t(DOWNLINK_ONLY)) v = eScheduledCommunicationType::ScheduledCommunicationType_DOWNLINK_ONLY;
        if (s == utility::conversions::to_string_t(UPLINK_ONLY)) v = eScheduledCommunicationType::ScheduledCommunicationType_UPLINK_ONLY;
        if (s == utility::conversions::to_string_t(BIDIRECTIONAL)) v = eScheduledCommunicationType::ScheduledCommunicationType_BIDIRECTIONAL;

        setValue(v);
    }
    return ok;
}

ScheduledCommunicationType::eScheduledCommunicationType ScheduledCommunicationType::getValue() const
{
   return m_value;
}

void ScheduledCommunicationType::setValue(ScheduledCommunicationType::eScheduledCommunicationType const value)
{
   m_value = value;
}


}
}
}
}


